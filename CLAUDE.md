# CLAUDE.md — 曲げシミュレーター（株式会社高橋鉄骨）

## 「続きをお願い」と言われたら

1. `git pull`（別の端末・GitHub直アップで更新されていることがある）
2. 共有フォルダの `引き継ぎ.md` を読む。「いまの状態」と「残っている作業」から再開する
   `\\srv02\共有\データフォルダ\ユーザー共有用\地主\claudecode\曲げシミュレーション\引き継ぎ.md`
3. 何から再開するかを **1〜2行で** ユーザーに伝えてから作業する

## 何か進めたら（2026-09-22 ユーザー指示）

別アカウントでもすぐ続きができるように、**進めるたびに必ず**やる：

1. 画面で動作を確かめる
2. `引き継ぎ.md`（共有フォルダ）の「いまの状態」「残っている作業」を書き直す
3. commit して push する（この作業については毎回聞かなくてよい）

**パラメーターの登録**（金型・台の寸法、片伸び、実績値など）は、必ず別アカウントでも見える場所に置く。

| 登録するもの | 置き場所 |
|---|---|
| プログラムが使う値（金型・台・片伸び表・機械の型など） | リポジトリに入れて commit・push |
| 品物の寸法・実績・段取り | 共有フォルダ（`bendsim.json` か、このフォルダのファイル） |

ブラウザの中（localStorage）やこのPCだけのフォルダ（ダウンロードなど）に置いたままにしない。

## ユーザーとの約束（必ず守る）

| 約束 | 中身 |
|---|---|
| 説明は短く・日本語 | 結論だけ。数字が3つ以上なら表。自分で作った言葉（例：台の「首」）は使わない |
| 先走らない | 画面で目視確認できるまで「完了」と言わない。確認できたら commit・push まで（上の「何か進めたら」） |
| 金型を勝手に変えない | 選んだ金型はそのまま。板厚・材質を入れたときだけ基準金型を選ぶ |
| ありえない型を出さない | 機械ごとの型（`MACHINE_DIES`）以外は候補にも出さない |
| 印刷物はExcel | 現場はExcelで開く。A3で1枚にきれいに収める |
| ボンデ＝鉄 | 曲げではボンデは鉄の表を使う。材質を足さない |
| 可否判断シートは編集しない | `docs/曲げ可否判断シート.html` はダウンロード版を置くだけ。直したいときは生成元の会話へ渡す文面を作る（安全に関わる誤りだけ例外） |
| 寸法・実績は社内に | リポジトリは公開。寸法・実績・引き継ぎは共有フォルダに置き、リポジトリに入れない |

## 共有フォルダ（寸法・実績・引き継ぎ）

`\\srv02\共有\データフォルダ\ユーザー共有用\地主\claudecode\曲げシミュレーション`

| ファイル | 中身 |
|---|---|
| `引き継ぎ.md` | いまの状態・残っている作業 |
| `bendsim.json` | シミュレーターが保存する段取りと実績。`current`＝最後の段取り、`saves`＝名前付き保存、`records`＝実績 |
| `履歴\bendsim_YYYYMMDD.json` | その日最初の保存の前の中身（壊れたら戻す） |
| `実績シート\` | Z曲げ・コの字の実績記入シート（xlsx） |

- シミュレーターは会社PCの Chrome・Edge で、右上「共有フォルダ保存」からこのフォルダを選ぶ。開くと最後の段取りが戻る
- iPhone・社外からは見るだけ（保存されない）
- Claude は上のパスを直接読み書きしてよい

## 構成

| 場所 | 中身 |
|---|---|
| `bending-simulator.jsx` | 本体（判定エンジン＋画面）。check.html もここの関数を使う |
| `src/records.js` | 曲げた／曲げられなかったの実績（PCのブラウザにも保存） |
| `src/cloud.js` | 共有フォルダ保存（ブラウザのフォルダ読み書き機能） |
| `src/check.jsx`, `check.html` | かんたん判定ページ |
| `public/dash/` | Webで開く確認資料（ダッシュボード・ダイ土台一覧） |
| `docs/` | 配布資料 |
| `scripts/` | シート作成・DXF出力などのNode/Pythonスクリプト |
| `経緯まとめ.md` | 開発の全経緯（長い。必要な章だけ読む） |

- 公開URL：https://jinu-h420.github.io/bending-simulation/ （main に push すると約40秒で反映、`.github/workflows/deploy.yml`）
- かんたん判定：https://jinu-h420.github.io/bending-simulation/check.html
- 確認資料：https://jinu-h420.github.io/bending-simulation/dash/
- 起動：`npm install` → `npm run dev` → http://localhost:5173/
- Node スクリプトは jsx から判定エンジンを切り出して使う（先頭の import 行を除いて評価）
