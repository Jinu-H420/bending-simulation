# 棚卸し：bending-simulator.jsx への移植 ＋ 4曲げ以上の設計案

作成: 2026-09-09
対象: 宿題1（jsx への移植）の完全な差分棚卸し／宿題4（4曲げ以上）の設計案

前提: `docs/曲げ可否判断シート.html`（以下「シート」）の幾何エンジンが正。
`bending-simulator.jsx`（以下「jsx」）は第7章までの状態で、第8〜23章の成果が入っていない。
シート内のコメント `/* 幾何エンジン（bending-simulator.jsx と同一）*/` は**現時点で誤り**。

> **2026-09-10 追記** — 以下の各表の「jsx」列と、節ごとの状態注記（A-1まとめなど）は
> **マージ前（7月版 jsx）の姿**であり、分岐の記録としてそのまま残してある。
> 2026-09-10 にローカル版とマージした結果、**E3・E4・E5・E6・E16・E18 と E10 の台4通りは
> 既に jsx へ入っている**。現在の状態は `経緯まとめ.md` 第25章を参照。
> 可変の状態欄である A-6 のフェーズ表だけは最新に更新してある。

---

## A. 宿題1 — jsx とシートの差分（項目別）

「工数」は 極小 / 小 / 中 / 大 の4段階（小＝〜30分、中＝半日、大＝1日〜）。

### A-1. コア幾何（ここが全ての土台）

| # | 機能 | シート | jsx | 移植の要点 | 工数 |
|---|---|---|---|---|---|
| E1 | `computeChain` の伸び `grow`（＝片伸び − t/2）<br>第11章 | 引数 `grow`。`ms = ms0.map(...wOf...)` で曲げ済み／進行中の辺を伸ばす。`vy = vHalf·tan(α) − (t/2)/cos(α)`、`innerY` も返す | `grow` なし。`vy = −t/2 + d`。`innerY` なし | `computeChain` を丸ごと差し替え。戻り値に `vIdx`・`innerY` を追加。`part.grow` を `effSegs` から計算し呼び出し5か所へ | 中 |
| E2 | 刃先Y ＝ `innerY`（マイター点）<br>第8.1章 バグ3 | `tipY = ch.innerY − lift` | `buildPunch(…, ch.vy − t/2, …)`（0.66mm 低い） | E1 に含まれる（`innerY` 導出）。`buildPunch` 呼び出し3か所を修正 | 小 |
| E3 | V半幅 ＝ V壁を型上面まで延長した交点<br>第8.1章 バグ1 | `die.vHalf` は取付図の実測交点 | `DIE_LIB[].grooves[1]` は肩R外端。`v12stack` は `vHalf:6` 決め打ち | 実測がある型（下記E10の5型）だけ交点を持たせる。他は「支点は近似」と明示（現状の `manual:true` と同じ扱い） | 中（データ） |
| E4 | 最小フランジ `shoulderReach(vHalf,t,ang)`<br>第8.2章 | あり。`evaluate` / `zFeasible` が使用。90°で ≒ 0.707·V幅 | なし（`dieCandidates` に `vHalf+1 < minFlange` の粗フィルタのみ） | 関数追加（3行）。工程判定・自動探索・画面表示に組込み | 小 |
| E5 | 弧長ベースの除外 `splitChain`<br>第12章 | `minGap(…, exArc)` が板に沿った弧長 `need+t` 未満だけ除外 | 座標距離 `excl = [[−vHalf,0],[vHalf,0],[0,vy]]`、半径 `exR = t·1.8+1.5` | `splitChain` ＋ `sampleInBox` ＋ `clipSeg`（Liang–Barsky）＋ `bboxOf` を移植 | 中 |
| E6 | `minGap` が「最小すきま(mm)」を返す（負＝食い込み量） | あり | `detectCollisions` は当たり点の**配列**だけ。余裕mmを出せない | `detectCollisions` を `minGap` 相当に置換。判定表示を「余裕◯mm／食い込み◯mm」に | 中 |
| E11 | `mirrorClose` の index0 バグ<br>第9.3章 | 該当なし（閉多角形を直接持つ） | `for(i=len−1; i>=1; i--)` が index0 の鏡像を飛ばす。中心線上にない輪郭で天面が斜めに閉じる | `>=1` → `>=0`＋先頭/末尾の重複頂点を除去 | 極小 |

**A-1 まとめ**：E1・E2・E11・E4・E5・E6 で1セッション。ここまでで jsx の判定がシートと同じ土俵に乗る。

**✅ 2026-09-09 完了**（経緯まとめ 第24章）。E3（V半幅の実測交点）だけはデータ移植（フェーズ2のE10と同時）
に据え置き、上表のとおり本フェーズの対象外のまま。E1の`growArr`はK係数式からの暫定近似で、
実測の片伸び表（E16・フェーズ3）に未対応。

### A-2. 上型ホルダと理由E

| # | 機能 | シート | jsx | 移植の要点 | 工数 |
|---|---|---|---|---|---|
| E7 | 上型ホルダ `placeHolder`（`ホルダ.dxf` 実測 36点）<br>第13章 | `D.holder`。`tipY − (pu.H + h)` で位置決め（タング下端＝ホルダ最下端） | なし | `ホルダ.dxf` 座標（36点）を定数化。`placeHolder` 移植（3行） | 小 |
| E8 | ホルダ上の柱 `placeRam` 幅115mm<br>第16〜17章 | `HOLDER_W = 115`、`RAM_UP = 4000`。ヤゲン頂部＋2mm から上を115mm幅の柱に | なし（別物の `machineCheck` はある） | `placeRam` 移植（8行） | 小 |
| E9 | 理由E判定（ホルダ・柱への食い込み）<br>第13章 | `evaluate` が `up` / `atU` を返す。`maxH1()` で上限を逆算 | なし。旧 `machineCheck`（ストローク／出し入れ）が残存 — **これは第10.5章で廃止された旧・理由D/E**。OH の基準面が未確定のため | ① `machineCheck` を撤去または「参考値」に格下げ ② 工程判定に理由E（ホルダ＋柱の `minGap`）を追加 | 中 |
| E10 | 実測台 `die.polys`（4型＋V63 の階段輪郭）<br>第9〜10章 | `D.dies[].polys` に直接。該当しない型は台なし表示 | `resolveDie` の `INSERT_STACK_RIGHT` / `DIE_RIGHT_HALF` は bending.dxf の一般化＝**推測**（第9.1章で「実物とまったく違う」と判明したもの） | シートの `D.dies[].polys`（974061／982061／03500／03600／V63）を移植。jsx の23型は台を出せるのを実測5型に限定し、他は「台なし・参考」 | 中（データ） |
| E20 | 下逃げ（理由C）`D.DOWNMAX`＋専用UI<br>第9.2章 | ダイ上面からの深さ別・片側逃げ量の表と専用チェッカー | ダイ下側の当たりは `die.polys` 経由で部分的にのみ | 台が実測（E10）になれば自動でカバー。専用の深さ表は任意 | 小 |

**A-2 まとめ**：E7〜E10 で1セッション。`ホルダ.dxf` と実測台のデータ移植が中心。

### A-3. テーブル類（データ移植が中心）

| # | 機能 | シート | jsx | 工数 |
|---|---|---|---|---|
| E13 | Z段差の最小＝実績優先 `D.ZMIN` ＋ `zMinCalc`（＝3×(内R+t)）<br>第21〜22章 | あり。実績を下回れば計算に関係なく✕ | なし | 小 |
| E16 | 材質（鉄／縞／ボンデ）と片伸び表 `D.NOBI`（折り曲げ表 2024-07-26 版の実読み）<br>第18章 | 実測表を引く | `bendDeduction`（K係数式）で自前計算 | 中（方針判断あり — 下記） |
| E17 | 基準金型の自動選択 `pickDie` / `D.BASEV`（xlsx 赤枠の実読み）<br>第14〜15章 | あり | `dieCandidates` が `V≒8t` で機械的に選ぶ | 小 |
| E18 | 14型の曲げ内R・所有台数 `D.DIETBL`、V63 の断面<br>第22章 | あり | `DIE_LIB` に V63 なし | 小 |

**方針判断（E16）**：シートは「折り曲げ表の実測値」、jsx は「K係数式」。
どちらかに寄せる必要がある。実務は表に載っている値で指示するので**表を正**にし、
表に無い組み合わせだけ式でフォールバックする案を推奨。

### A-4. Z曲げ・特殊ヤゲン

| # | 機能 | シート | jsx | 工数 |
|---|---|---|---|---|
| E12 | Z総当りの工程別裏返し<br>第23章 | `zSearch`：`[[0,1],[1,0]] × v0 × v1 × flip × mirror` ＝ 352通り | `searchSequences`：汎用DFS（bend順 × valley × mirror、**既に工程別**） | 中 |
| E14 | 特殊ヤゲン（中低）`clipLow` / `SPECIAL`、窓 L 判定<br>第18章 | あり。`punch.special` でホルダ除外 | なし | 中 |
| E15 | 捨て曲げ成立条件 `SUTE_MIN_INNER = 120`<br>第17章 | ✕でも内-内120以上なら「捨て曲げなら作れる」 | なし | 極小 |

**E12 について**：jsx の `searchSequences` は既に工程ごとに `valley` / `mirror` を振る DFS なので、
シートの `zSearch` が第23章で到達した「工程別裏返し」を構造的にはすでに満たしている。
Z特化の谷曲げ判定（`activeDirOK`）の扱いを合わせるだけ。

### A-5. jsx が既に持っていて有利な点（移植不要）

| 機能 | 状態 |
|---|---|
| 可変辺数UI（＋辺追加／－削除） | あり。シートは H1/W/H2 の3辺固定 |
| 曲げごとの角度・山谷入力 | あり。シートは90°固定 |
| 曲げ順の汎用DFS `searchSequences` | あり。シートは Z 専用の総当りのみ |
| 金型総当り探索 `searchTools` | あり |
| ストローク再生・パン・ズーム・ガイド線 | あり。シートは静止断面図のみ |
| 23型のダイ・11本のヤゲン・中間板2種 | あり。シートは実測5型のみ |

→ **jsx を捨てる必要はない**。むしろ「多曲げ・アニメ・広い金型ライブラリ」は jsx の強み。
シートの「実測に基づく正確な幾何・ホルダ・理由E」を jsx へ持ち込めば、jsx が上位互換になる。

---

## A-6. 移植の順序と工数見積り

| フェーズ | 内容 | 工数 | 状態 |
|---|---|---|---|
| 1 | E1・E2・E11・E4・E5・E6（コア幾何） | 1セッション | ✅ **完了 2026-09-09**（経緯まとめ 第24章）。`grow`／`innerY`／`mirrorClose`修正／`shoulderReach`・`reachCheck`／`minGap`（弧長除外）を実装。数値検証済み（Z曲げ中央辺 8.3→9.15→10.00 が第11章の実測と一致） |
| 2 | E7・E8・E9・E10・E20（ホルダ・実測台・理由E） | 1セッション | 🔶 **一部完了 2026-09-10**（経緯まとめ 第25章）。E10 の実測台は `DIE_MOUNT` として4通り（974061／982061／03500／03600）が入っている。**残り**：E7 `placeHolder`、E8 `placeRam`、E9 理由E（旧 `machineCheck` が12箇所残存）、E20 `DOWNMAX`、E10 のうち V63 の台 |
| 3 | E13・E16・E17・E18（テーブル移植） | 1セッション | 🔶 **一部完了 2026-09-10**（経緯まとめ 第25章）。E16 の実測片伸び表 `NOBI_TABLE`・最小外寸 `MINOUT_TABLE`・材質（鉄／縞）と、E18 の V63 断面・曲げ内R `V_INNER_R` が入っている（E1の`growArr`はK係数式ではなく実測表を引く形に置き換わった）。**残り**：E13 `ZMIN`、E17 `pickDie`／`BASEV`、E18 の所有台数 `DIETBL` |
| 4 | E12・E14・E15（Z細部・特殊ヤゲン・捨て曲げ） | 0.5〜1セッション | 未着手 |

合計 **3.5〜4セッション**。
別途、シートの回帰セット（PL3.2 コの字100³ → ✕38.4mm、PL6 500³ → ✕、曲3.dxf Z → ○ 2曲げ目0.0mm ほか）を
jsx のテストケースとして移植すること。

### 長期的な結論（今回は着手しない）

上記を毎回2ファイルに手で入れると、また乖離する。
最終的には**幾何エンジンを1ファイル（例 `src/engine.js`）に抽出し、シートと jsx の両方が import する**構成が正しい。
シートは単体HTML縛りがあるのでビルド時にインライン展開するか、`<script src>` で読む形になる。
移植（フェーズ1〜4）を終えた時点で、その版を engine.js として切り出すのが手戻りが少ない。

---

## B. 宿題4 — 4曲げ以上の判定をシートに組み込むか

### B-1. 現状の整理

シートが扱えるのは**名前のついた形だけ**：
- L（1曲げ）／コの字・ハット・箱（2〜3曲げ・H1/W/H2 モデル）／Z（2曲げ・逆向き）

`evaluate` は `segs:[H2,W,H1]`・2曲げ固定、`zFeasible` は2曲げ逆向き固定。
4曲げ以上（トレー、二重ハット、段違いブラケット、多段曲げ）は入力も判定も対応外。

一方 **jsx の `computeChain(part, seq, …)` は `segs[]`＋`bends[]` で N 曲げ対応済み**。
シートはこれを2曲げに固定して呼んでいるだけ。つまりエンジンの土台はある。

### B-2. 案（3択、B1 推奨）

**B1（推奨）— 断面プロファイル入力＋曲げ順の総当り**
- 入力：`t` / 材質 / 下型 / 上型（or 自動）＋ 辺の列 `[L1, (θ1,山/谷), L2, (θ2,山/谷), …, Ln]` ＋ 寸法の取り方（外／内／展開）
- 展開換算：既存の `grow = 片伸び − t/2` を N 曲げに。隣接曲げ数は端=1・中間=2（`zFlat` が既にこの形）
- 判定：曲げ順を DFS で総当り。各工程で ①V肩に届くか ②ヤゲン ③ホルダ・柱 ④ダイ・台（下逃げ）⑤曲げ切り（ストローク）。`zFeasible` のステップ判定を N 工程へ一般化するだけ
- 姿勢：工程ごとに mirror（左右）と valley（裏返し）を自由に（第23章で確立済み）
- 枝刈り：各工程で reach 不足 or `minGap < 0` なら即打ち切り。N≦6 で数秒。それ以上は「DXF から取り込み（B3）」へ誘導
- 出力：成立した曲げ順の手順書（「工程1: 曲げ線3を谷、工程2: …」）＋ 工程送りの断面図（既存 `zDraw` を流用）

**B2 — 既知パターンの追加（二重ハット・トレー・段2つ）だけ**
- 実装は軽いが任意形状は無理。B1 の劣化版。**不要**

**B3 — DXF／展開図読み込み（step5 の本筋）**
- `bending-sim-step4.html` が既に「曲げ線で分割＋各面の折り曲げ角度」を JSON（`{t, segs, bends}`）で出す
- 4曲げ以上は「手入力がつらい」問題でもあるので、DXF から取るのが実務的に正しい
- B1 のエンジンに step4 の JSON を流す入口を1つ足すだけ

### B-3. 推奨する進め方

1. **B1 を土台**にする（断面プロファイル入力＋曲げ順DFS）。エンジンは `computeChain` が対応済みなので、
   作業は「探索ループの一般化」＋「可変辺数UI」＋「工程送りの結果表示」が中心。**中〜大（1.5〜2セッション）**
2. **B3 を入口として併設**。step4 の出力形式が確定していれば **＋0.5セッション**
3. B2 は作らない

### B-4. 4曲げ以上でも断面では見えないもの（現状と同じ限界。明記する）

- 奥行き方向のフランジ同士の干渉（2D断面の外）
- バックゲージ・機械フレームとの干渉
- フランジの最大長（自重・持ち方・機械の奥行きで決まる。実績が要る）
- スプリングバック、内Rの実値（段差の最小は第21〜22章で実績値を優先する形に移行済み）

### B-5. B と A の関係

B1 を jsx 側でやれば、A（移植）と一体で進む——
移植でコア幾何を揃えたあと、jsx の `searchSequences`（既に多曲げDFS）に
ホルダ・理由E・実測台を通すだけで B1 相当になる。
**シート単体で B1 をやるより、A のフェーズ1〜2を終えてから jsx で B1 をやるほうが総工数は少ない。**
ただしシートは現場配布物なので、シートにも簡易版（B1 の判定だけ・アニメなし）を残す判断はありうる。
