# 板金曲げシミュレーター開発 — 経緯まとめ

最終更新: 2026-09-09

---

## 0. プロジェクトの目的

株式会社高橋鉄骨（埼玉県和光市）のアマダ製ベンディングマシンで、
**「指定された金型と板金形状で、物理的に干渉せずに曲げ加工が可能か」**を
実機に鋼板を投入する前にPC上で検証する。

実機のAMNC画面に入力してシミュレーションするのではなく、
**パソコン上だけで「曲がるか」を確認したい**というのが要求の核心。

---

## 1. 成果物

| ファイル | 内容 | 状態 |
|---|---|---|
| `bending-simulator.jsx` | メインの2Dキネマティクス・シミュレーター（React） | 運用可。コア幾何は`docs/曲げ可否判断シート.html`と同期済（第24章・フェーズ1）。ホルダ・実測台等は未移植 |
| `docs/曲げ可否判断シート.html` | 5型の曲げ可否判断シート。幾何は最新（第8〜23章すべて反映） | 運用可 |
| `bending-sim-step3.html` | AP100のDXFを読み込む単体HTMLツール（ステップ3） | 完成・バグ修正済 |

---

## 2. 金型データの取り込み（最重要・最も手戻りが多かった工程）

### 2.1 経緯と失敗

| 段階 | 入力 | 結果 |
|---|---|---|
| ① Gemini版 | 寸法を推定 | 金型形状が図面と不一致 |
| ② PDF（V12.pdf / ヤゲン.pdf） | ベクタ線分を抽出 | ダイは正確。**ヤゲンを誤読** |
| ③ ユーザー指摘 | 「太線は中間板、細線がヤゲン」 | 修正するも形状なお不一致 |
| ④ **DXF（色情報つき）** | 色3＝緑＝ヤゲン | **推測ゼロで確定** |

**教訓**: PDF変換すると輪郭線・寸法線・引出線・ワーク線がすべて同じ細線に潰れ、
どれが工具かを座標から推測するしかなくなる。**DXFなら色・レイヤーで確実に判別できる。**

私が「R（円弧）」と解釈していた先端形状も、実際は全て直線だった（PDF上の短い直線3本を
逃げカーブと誤読していた）。

### 2.2 確定した金型ライブラリ

すべて `bending.dxf` / `ヤゲン.dxf` / `金型.dxf` / `50001.dxf` のベクタ座標をそのまま採用。

**ヤゲン（パンチ）11本**
```
00300（先端R6・丸刃／DXFのARCを12分割近似）
00402 / 01002 / 04502 / 04702 / 10300 / 10400 / 10600 / 71606 / 80306 / 904061
```
- `10300` = `10400` = 旧`01003` = `default.dxf` は断面同一（長さ違いと推定）
- `904061` の例: 刃先86°（両面9）→ 左面 段1.5 → 47°×21.9 → 垂直100.9、全高150.1
- `00300` 以外はすべて直線のみ（R無し）

**ダイ23個**
| 種別 | 品番 |
|---|---|
| Vダイ単体 | 03500(V35.5) / 03600(V43.5) / 03700(V53.5) / 03800(V67) / 01360(V85) / 01860(V106) / 03900(V133) / 01400(V170) / 34300(V47鋭角) |
| 2溝ダイ | 30540(V12+V20) / 30640(V16+V25) ← 使う溝を選択可 |
| Vインサート | 970061(V6) / 974061(V12) / 977061(V16) / 979061(V20) / 982061(V25) ← 単体／スタック取付の両方選択可 |
| 特殊 | 08100・08150(サッシU14) / 05500(Uチャンネル60) / 33075(段曲げ) / 30000(凸50) / 3009(凸20) / 825(フラット板) |
| 実機 | V12段付きスタック（bending.dxf・全高356.9） |

**中間板2種**
- `std`: 122.7 × 78.3・タング9.1・非対称（bending.dxf）
- `50001`: 左右ほぼ対称・スロット深さ32（50001.dxf）

→ どのヤゲンとも組み合わせ可。スロット天井を工具頂部に自動整合させ、**一体で昇降・干渉判定**。

---

## 3. シミュレーターの実装内容（`bending-simulator.jsx`）

### 3.1 キネマティクス
- **エアベンディングの正確な幾何計算**: V溝の肩を支点として板が滑り込みながら回転（スイング）
- 単なる図形の上下移動やモーフィングではない
- 工程ごとに変わる板の基準位置（反転・セット位置変更）を再現

### 3.2 検証機能
- ストローク0〜100%スライダーによるリアルタイム干渉チェック
- 干渉点を橙色で可視化
- **ガイド線**: 曲げ開始前（ストローク0%）の板位置を破線で重ね表示 → AMNC画面との目視比較用
- ドラッグでパン、ホイールでズーム（刃先／標準／全体プリセット）

### 3.3 AP100 / Dr.ABE_Bend 相当の機能

| AP100等の工程 | 本シミュレーターの実装 |
|---|---|
| ①展開図作成・曲げ属性付与 | 「外寸→伸び値計算」モード。仕上がり外寸＋内R＋K係数から**伸び値（曲げ控除）を自動計算**し展開値へ換算<br>`BD = 2(R+t)tan(θ/2) − θrad(R + K·t)` |
| ②立体姿図の簡易干渉チェック | （2D断面のため対象外・後述の限界を参照） |
| ③金型選定・曲げ順決定 | **「この金型で曲げ順を自動探索」**: 曲げ順×姿勢（左右反転・表裏）をDFSで総当り<br>**「金型も含めて総当り探索」**: 経験則（4t ≦ V幅 ≦ 20t、V半幅+1 < 最短フランジ）でダイ候補を絞り、全ヤゲン×向きを探索 |
| ④3Dアニメによる干渉確認 | ストローク再生＋干渉点表示（2D断面） |
| ⑤実機送信 | 対象外（本ツールは実機投入前の確認が目的） |

### 3.4 機械プリセット

| 機種 | オープンハイト | ストローク |
|---|---|---|
| AMADA HD3504NT (350t×4,000) | 620 | 350 |
| AMADA HG2203 (220t×3,000) | 520 | 250 |
| AMADA EGB1303E (130t×3,000) | 520 | 250 |
| 手動入力 | 任意 | 任意 |

**自動判定される項目**:
- **曲げ切り判定**: ダイ高＋パンチ組立高に対しストロークが足りて90°まで押し込めるか
- **出し入れ判定**: 上死点の刃先〜ダイ上面のすき間に、曲げ済み部品の張出し高さが収まるか

> 実機はクランプ形式や中間板の取付高さで実効オープンハイトが変わるため、**「OH補正」欄**で±調整できる。
> 現場で一度「刃先〜ダイ上面」を実測して合わせれば、以後その機械の実態どおりに判定される。

---

## 4. AP100 DXF読み込みツール（`bending-sim-step3.html`）

AP100の内部ソースは参照できない（有償の専用ソフト）が、**AP100が出力するDXFを読めば同じことができる**。

### 4.1 ステップの進行

| ステップ | 内容 | 状態 |
|---|---|---|
| 1 | DXFのLINE/POLYLINE座標をコンソール出力 | 完了（100×100の正方形で検証） |
| 2 | Canvasに自動フィット描画（バウンディングボックス→センタリング→Y軸反転） | 完了 |
| 2b | ARC/CIRCLE/bulge対応 | 完了 |
| 3 | 曲げ線の自動検出（色・線種による分類） | 完了 |
| 4 | 曲げ線を基準にした展開図の分割・折り曲げ角度の表示 | **未着手** |

### 4.2 技術的な要点

**CDN読み込み**: `dxf-parser@1.1.2` はグローバル変数を作らないため、
`<script src=...>` では `DxfParser is not defined` になる。
`<script type="module">` 内で `import()` するのが正解。

```javascript
const mod = await import('https://cdn.jsdelivr.net/npm/dxf-parser@1.1.2/+esm');
const cls = mod.DxfParser || mod.default;   // バージョンでexport形式が異なる
```

**円弧対応**: 板金部品はコーナーR・丸穴・長穴だらけなので必須。
- `ARC` / `CIRCLE` を線分列に分割（弦誤差から分割数を自動決定）
- `LWPOLYLINE` の `bulge` 値（頂点間を円弧でつなぐ指定）を円弧として復元
- `bulge = tan(内角/4)`、正 = 反時計回り

**曲げ線の判定**:
| 色（ACI） | 線種 | 分類 | 描画 |
|---|---|---|---|
| 7（白） | 点線系 | **確定** | 赤の点線 |
| 7（白） | 実線 | 候補 | 黄の点線 |
| 非白 | 点線系 | 候補 | 黄の点線 |
| 非白 | 実線 | 外形線 | 青の実線 |

エンティティ自身の指定が BYLAYER の場合はレイヤーテーブルから解決。
片方だけ一致するものを「候補」として黄色表示するのは、**黙って見落とさないための安全策**。

### 4.3 発見・修正したバグ（重要）

**`bulgeToPoints` の弧の回転方向の符号が逆だった。**

```javascript
// 修正前（バグ）
const a = startAngle + theta * (i / n);
// 修正後
const a = startAngle - theta * (i / n);
```

検証結果（終点が p1 = (20, y) に一致すべき）:

| パターン | bulge | 修正前の終点 | 期待 | 判定 |
|---|---|---|---|---|
| A 浅いR | 0.101015 | (-18.4, -37.8) | (20, -30) | **✗ 全く違う場所** |
| B 半円 | 1.0 | (20, -60) | (20, -60) | ✓ たまたま一致 |
| C 深いR170° | 0.916331 | (19.7, -93.5) | (20, -90) | **△ 不一致** |

**半円（180°）のときだけ符号を反転しても同じ点に戻るため、見た目上は動いているように見えて発覚しにくいバグだった。**
実際には180°以外のすべてのR（＝板金で最も頻出するコーナーR）で外形が歪む。
修正後は3パターンとも誤差1e-6未満で一致することを確認済み。

なお**分類ロジック（確定／候補の振り分け）とBYLAYER解決は検証の結果、問題なし**だった。
CIRCLE（丸穴）が曲げ線と誤認されることもない。

---

## 5. 現在の限界・未解決事項

### 5.1 シミュレーターの限界
- **2D断面のみ**。曲げ線に直交する断面の干渉しか見えない
- 対象外: 奥行き方向のフランジ同士の干渉、バックゲージとの干渉、機械フレームとの干渉
  → バックゲージの位置・形状データがあれば拡張可能

### 5.2 確認待ちの項目

**(a) dxf-parser のレイヤーテーブルのキー名**
コードは `layerTable[layer].colorIndex` / `.lineType` を想定しているが、実機未確認。
ブラウザのコンソールで以下を確認する（DXFを読み込むと自動で出力される）:
```
レイヤーテーブル: {"0":{...},"OUTLINE":{...},"BEND":{...}}
```
キー名が違えば `resolveColorIndex` / `resolveLineType` の2行を書き換えるだけ。

> 注: 検証環境にネットワークが無くdxf-parser本体を実行できなかったため、この1点だけ未確認。

**(b) 寸法の測り方**
仕様書では仕上がりが「左フランジ32／中央ウェブ10／右フランジ39.7」、展開が86.3（30/16.3/40）。
ウェブ 16.3 → 10 の差は、10がフランジ内側間（Z段差の内法）だからと理解しているが、
外寸モードの伸び値計算は単純外寸積み上げなので、**御社が実際にどの測り方で指示するか**（外寸／内寸／段差高さ）
に合わせて入力モードを調整する必要がある。

**(c) 中間板とヤゲンの組み合わせ規則**
現状はどのヤゲンとも自由に組み合わせ可。実際に決まりがあれば選択肢を絞れる。

---

## 6. 補足: リポジトリについて

このチャットのClaudeは**リポジトリにアクセスできない**。
ファイルの中身を作って会話内で渡すところまでが担当範囲。

BRAIN2 / mitumori などのリポジトリを操作しているのは、
**あなたの手元のPCで動いている別のツール（Claude Code）**。
現在のリポジトリを確認したい場合は、そちらのターミナルで:

```bash
pwd              # 現在のディレクトリ
git remote -v    # リモートリポジトリのURL
git status       # 変更状況
git log --oneline -5   # 直近のコミット
```

---

## 7. 次のステップ候補（TODO）

| # | 項目 | 状態 |
|---|---|---|
| 1 | ステップ4a: 曲げ線基準の展開図分割・断面(フランジ長)抽出 | ✅ 完了 (bending-sim-step4.html) |
| 1b | ステップ4b: 曲げ角度・山谷の自動読み取り | ⚠️ 現状は手入力。AP100の実DXF待ち |
| 2 | ステップ5: step4のJSONを bending-simulator.jsx に接続 | ❌ 未着手（受け渡し形式は確定: {t, segs, bends}） |
| 3 | バックゲージのデータ取り込み（干渉判定の対象を拡張） | 🔧 拡張候補 |
| 4 | 実機での実効オープンハイト測定 → OH補正値の確定 | 🔧 現場作業（9/2までの宿題） |
| 5 | 幾何モデルのバグ3件（V半幅・中立軸オフセット・刃先高さ） | ✅ 完了 2026-08-27（第8章） |
| 6 | 最小フランジ判定（V肩に届くか）の組み込み | ✅ 完了 2026-08-27 |
| 7 | 項目6「曲げ可否条件」の資料化 | ✅ 完了 docs/曲げ可否判断シート.html |
| 8 | ダイの台（ホルダ・ベース）を干渉判定に追加 | ✅ 完了 2026-08-27（第9章） |
| 9 | mirrorClose の閉じ方バグ | ✅ 完了 2026-08-27 |
| 10 | 台を実測形状（05500ホルダ＋ベース）に差し替え | ✅ 完了 2026-08-27（第9章・dxf/曲げシミュレーション.dxf） |
| 11 | 03900・01400 の取付と、機械3台で台が共通かの確認 | 🔧 現場確認 |
| 12 | 4型（V12/V25/V32/V40）を実測で組み直し | ✅ 完了 2026-08-27（第10章） |
| 13 | 接触を干渉と誤判定していた件（しきい値0.05mm） | ✅ 完了 2026-08-27 |
| 14 | 5型目以降の追加（取付図待ち） | 🔧 図面待ち |

※ ステップ4aの検証: Z曲げ展開図(86.3 = 30+16.3+40)で segs を復元できることを、
  軸に沿った向き・30°回転+平行移動の両方で確認済み(誤差1e-7未満)。
  切欠きで分断された同一直線上の曲げ線が1本に統合されること、
  非平行な曲げ線はエラーを返すことも確認済み。
  検証用DXFは dxf/test/ に格納。

---

## 8. 2026-08-27 の修正（幾何モデルのバグ3件）と項目6の資料化

### 8.1 修正したバグ

いずれも `bending-simulator.jsx` の幾何モデル側。健全な形状まで「干渉あり」と誤判定していた原因。

**(1) 溝の V半幅に肩R外端の座標を使っていた**

`DIE_LIB[].grooves[1]` はV壁ではなく**肩Rの外端**のx座標だった。
カタログ呼称より 8〜11% 大きく、エアベンドの支点位置がずれていた。

| 品番 | カタログ | 旧 vHalf×2 | 修正後 |
|---|---|---|---|
| 03500 | V32 | 35.5 | **32.0** |
| 03600 | V40 | 43.5 | **39.9** |
| 03700 | V50 | 53.5 | **50.0** |
| 03800 | V63 | 67.4 | **63.0** |
| 01360 | V80 | 85.3 | **80.0** |
| 01860 | V100 | 106.2 | **100.0** |
| 03900 | V125 | 133.4 | **125.0** |
| 01400 | V160 | 170.2 | **160.1** |
| 34300 | V40鋭角 | 46.7 | **40.0**（45°） |

→ `grooveVHalf()` を追加。溝内の傾斜直線（肩R・底Rの短い弦は除外）を型上面 y=0 まで
延長し、その最小 x を採る。**全19溝がカタログ呼称と一致**することを確認済み。
インサート（V8〜V25）と2溝ダイ（30540/30640）は元から一致していた。

**(2) 中立軸のオフセットが鉛直だった**

```js
const vy = -t / 2 + vHalf * Math.tan(alpha);   // 旧
const vy = vHalf * Math.tan(alpha) - (t / 2) / Math.cos(alpha);   // 新
```

板の外面がV肩に接するのがエアベンドの幾何。中立軸は外面から板厚半分だけ**面直**に
オフセットする。旧式は鉛直に t/2 ずらしていたため、深曲げで中立軸が t/2·(1−cos α) だけ
V壁側へ食い込んでいた（90°・t3.2 で 0.66mm）。

**(3) パンチ刃先の設置高さも同じ理由でずれていた**

`buildPunch(..., ch.vy - t/2, ...)` → `buildPunch(..., ch.innerY, ...)`。
刃先が当たるのは板の内側面の頂点＝マイター点なので、鉛直 t/2 ではなく (t/2)/cos α 上。
旧式ではパンチが 0.66mm 低く置かれ、刃先の斜面が板に食い込んでいた。

**修正前後の検証**：03500(V32)・03600(V40)・03700(V50)・03800(V63)・01360(V80) で
t1.6〜t6・フランジ8〜60mm の単純1曲げが**全通りNG**だったものが、修正後は全通りOK。
V12スタック・インサートは修正前後とも変化なし（vHalf が元から正しかったため）。

### 8.2 追加した判定：最小フランジ（V肩に届くか）

`shoulderReach(vHalf, t, angle)` を追加。曲げ頂点からV肩の接触点までの中立軸長さ。
90°では板厚にほぼ依らず **0.707 × V幅** に収束する。
これより短い辺は溝に落ちて曲がらないため、`stepFeasible()`（自動探索）と
画面上の工程判定の両方に組み込んだ。工程ボタンに「✕ フランジ不足(◯mm必要)」と出る。

社内のベンダー折り曲げ表の最小外寸は V×0.62〜0.82 で、この幾何限界より 1〜3mm 厳しい側。
**折り曲げ表の値をそのまま社内基準にしてよい**（安全側）。

### 8.3 成果物：曲げ可否判断シート

`docs/曲げ可否判断シート.html`（単体HTML・ダブルクリックで開く）
`docs/kanagata-data.json`（表の元データ）

顧客対応業務改善ミーティング 項目6「曲げ加工の可否条件（形状の制約）」への回答。
不可になる理由を4つに分類し、それぞれ数字で基準を出した。

| | 理由 | 基準 |
|---|---|---|
| A | フランジが短すぎて溝に落ちる | 最小フランジ＝折り曲げ表の最小外寸（≒0.7V） |
| B | 立ち上がった面がヤゲンに当たる | ヤゲン11本の逃げ包絡線（高さ別の張出し半幅） |
| C | Z曲げの段差が小さくて抜けない | ダイ11種×板厚6種の最小段差（総当り計算） |
| D | ストローク不足／出し入れ不可 | OH − ダイ上面高さ − ヤゲン組立高さ |

寸法を入れると4項目それぞれの「余裕何mm」を返す判定ツール付き。
判定にはこのリポジトリと同一の幾何エンジンを埋め込んでいる（近似式ではない）。

**分かったこと**：立上りは必ずヤゲンの細い側へ向ける。逃げ側は各ヤゲンとも高さに
かかわらず 2.8〜7.2mm で一定だが、背中側は高さ30mmで12〜27.6mmまで開く。
両側が立ち上がる箱・ハットは **10600（両側3.0mm・高さ66mmまで一定）の実質一択**。

### 8.4 未確定（9/2ミーティングの宿題）

- **ホルダ・ベースを含めたダイ上面高さ**。DXFで確定しているのはダイ本体の高さまで。
  テーブル面からの実測があるのは V12段付きスタック 356.9mm とインサート用スタック
  351.9mm のみ。単体Vダイ（03500〜01400）を載せたときの合計高さは、現場で
  「刃先〜ダイ上面」を各機1回実測すれば全金型で確定する。
- 現場で実際に起きたNG事例（当たった／抜けなくなった）の照合。4分類で足りるかの確認。

---

## 9. 2026-08-27 追記：ダイの「下側」を干渉判定に入れた

指摘：曲げのときに当たる／当たらないは、V溝だけでなく**ダイ本体の下**で決まる。

### 9.1 台のモデル（一度間違えて、実測で直した）

**最初にやったこと（誤り）**：bending.dxf のV12段付きスタックと INSERT_STACK_RIGHT が
ホルダ上面の高さ違いで一致したので、これを一般化して全ダイの下に付けた。
つまり単体Vダイの下にも「半幅14のホルダ」があるものとした。
→ **実物とまったく違う形になり、断面図が明らかにおかしいと指摘を受けた。**

**実測（`dxf/曲げシミュレーション.dxf`）**：この図面のダイ取付図に、
単体Vダイの本当の載せ方が描かれていた。

| 部品 | 実測 |
|---|---|
| ダイホルダ **05500** | 94幅 × 70高、上面に **60幅 × 15深** のスリット。側面が 30→47→30 に膨らむロブ形状 |
| ベース | 214幅 × 53高、両端タブで **248幅** |
| ダイの差込み | **15mm**（ダイ底面がスリット底に着く） |

**`DIE_LIB` の `05500`（"Uﾁｬﾝﾈﾙ60"）は、実はこのダイホルダと同一断面。**
取付図のジョー部分と座標が完全に一致する。ライブラリ上はダイとして登録されているが、
実際は中間ベース（ダイホルダ）。

深さ（ダイ上面から）と必要な片側逃げ量：

| 深さ | 逃げ量 | 何に当たるか |
|---|---|---|
| 0 〜 (本体高−15) | ダイ本体の半幅 | ダイ本体の肩 |
| +0 〜 +17 | 30 → 47 | ホルダのロブ（広がる） |
| +17 〜 +55 | 47 | ホルダ |
| +55 〜 +83 | 107 | ベース |
| +83 〜 +103 | 124 | ベース両端タブ |

**照合**：03500（60×60）＋05500＋ベースで、ダイ上面からベース下端まで **153.0mm**。
図面の実測値と一致。各段の幅（30 / 37 / 47 / 107 / 124）も全て一致。

実装：`DIE_HOLDER_RIGHT` / `DIE_BASE_RIGHT` / `HOLDER_SEAT=15` と
`dieFootHalf()` / `attachBase()`。`resolveDie(sel, vW, dieHalf, withBase = true)` に
第4引数を追加し、画面に「台（ホルダ・ベース）を含める」チェックボックスを設けた。
2溝ダイは選んだ溝がx=0に来るようずらしてあるので、ホルダはブロックの中心に付ける
（＝曲げ線に対して台が左右非対称になる。これも実態どおり）。

これにより `machineCheck` の `dieH`（ダイ上面高さ）も、単体Vダイで本体高さ止まり
（60mm等）だったものが台込みの実高さになる。

### 9.2 下逃げ量（型上面から深さ d まで下げるのに必要な、曲げ線からの片側距離）

| ダイ | d=0 | d=30 | d=50 | d=100 |
|---|---|---|---|---|
| 実機V12段付きスタック | **7.5** | 7.5 | 14.0 | 67.5 |
| 971561 V8インサート | **6.0** | 6.0 | 14.0 | 67.5 |
| 974061 V12インサート | **8.0** | 8.0 | 14.0 | 67.5 |
| 979061 V20インサート | **12.2** | 12.2 | 14.0 | 67.5 |
| 982061 V25インサート | **14.8** | 14.8 | 14.8 | 67.5 |
| 30540 2溝(V12側) | 42.5 | 42.5 | 64.5 | 124.5 |
| 03500〜03700 (V32〜V50) | **30.0** | 30.0 | 39.9 | 107 |
| 03800 V63・34300 V40鋭角 | **40.0** | 40.0 | 40.0 | 47〜107 |
| 01360 V80 | **47.5** | 47.5 | 47.5 | 47.5 |
| 01860 V100 | **60.0** | 60.0 | 60.0 | 60.0 |
| 03900 V125（台不明） | 77.0 | 77.0 | 77.0 | 77.0 |
| 01400 V160（台不明） | 92.5 | 92.5 | 92.5 | 92.5 |

**単体Vダイは深さ0の時点ですでに片側30〜92.5mm の逃げが要る。**
ブロックが溝の直下から同じ幅で立っているため、下がり量がゼロに近くてもこの幅ぶんの
距離が必要になる。曲げ線の近くで下へ回り込む形状は、段付きスタック（±7.5）か
インサート（±6〜14.8）でないと通らない。段付きスタックが「段付き」なのはこのため。

**未確定**：03900（底面90幅）と 01400（底面120幅）は 05500 の60幅スリットに入らない。
別の取付のはずだが図面がないため、台を付けず本体のみの値としている。
また機械ごとに台が違う可能性があり、取付図は1台ぶんしかない。

### 9.3 ついでに見つけたバグ：mirrorClose

```js
for (let i = right.length - 1; i >= 1; i--) pts.push([-right[i][0], right[i][1]]);
```

index 0 の鏡像を飛ばしているため、**先頭点が中心線上にない輪郭では天面が水平にならず、
(-x0, y1) → (x0, y0) の斜め線で閉じてしまう**。
`INSERT_STACK_RIGHT`（先頭 [14, 45]）がこれに該当し、ホルダ天面が斜めに欠けていた。
先頭点が x=0 のときだけ index1 から折り返すよう修正。
`DIE_RIGHT_HALF`・`PUNCH_STRAIGHT`・汎用フラットダイは先頭が x=0 なので影響なし。

### 9.4 資料の更新

`docs/曲げ可否判断シート.html` に **理由C「下がる面がダイ本体・台に当たる」** を新設し、
不可の分類を A〜E の5つに再編した。

| | 理由 |
|---|---|
| A | フランジが短すぎて溝に落ちる |
| B | 立ち上がった面がヤゲンに当たる（上） |
| C | **下がる面がダイ本体・台に当たる（下）** |
| D | Z曲げの段差が小さくて抜けない |
| E | ストローク不足／部品の出し入れ不可 |

理由Cには、19溝すべての断面図（台込み・実寸縮尺）、深さ別の下逃げ表、
「下がり量 Ld と曲げ線からの距離 Wd を入れると、どのダイなら受けられるか」を返す
判定ツールを載せた。

**未確定**：03900（V125）と 01400（V160）の取付。この2型だけ 05500 ホルダに載らない。
あわせて、機械3台で台が共通かどうか。

---

## 10. 2026-08-27 やり直し：4型に絞って実測だけで組み直した

### 10.1 なぜやり直したか

第9章の台は「bending.dxf のスタックを一般化」した**推測**で、実物と全く違っていた。
確認せずに作り込んで資料まで載せてしまい、使えないものになった。
以後は**図面から読めたものだけを使い、読めないものは載せない**方針に変更。

### 10.2 対象（実測がある4型だけ）

`dxf/曲2.dxf` のダイ取付図から実測。それ以外の金型はシートに載せない。

| 下型 | 品番 | 機械 | V幅 | 溝深さ | 深さ0の逃げ | 床まで |
|---|---|---|---|---|---|---|
| V12 | 974061 V12インサート | HG2203 | 12.00 | 6.43 | ±8 | 1061.5 |
| V25 | 982061 V25インサート | HG2203 | 25.00 | 13.40 | ±14.75 | 1061.5 |
| V32 | 03500 V32 | HD3504NT | 32.01 | 17.50 | ±30 | 1048.0 |
| V40 | 03600 V40 | HD3504NT | 39.93 | 21.39 | ±30 | 1046.6 |

**照合**：4型の断面・台とも図面と全点一致（差0.35mm未満）。
ヤゲン11本も曲2.dxfのヤゲン図と照合し最大差0.15mm。
V12＝±8・V25＝±14.75・ダイ本体＝±30 は `DIE_LIB` の値とも一致。

V12組立とV25組立は線分89本中79本が完全一致で、**違うのはインサートだけ**であることも座標比較で確認済み。

### 10.3 台の実装

`DIE_MOUNT` に、この4キーぶんの階段輪郭（ダイ上面=深さ0・V中心=x0 の右半分）を直接持たせた。
`mountPolys(sel)` が左右対称の閉じたポリゴンにする。**該当しない金型には台を付けない**
（`（台なし）` と表示）。第9章の推測コード（`HOLDER_RIGHT` / `attachBase` / `dieFootHalf`）は削除。

下逃げ量（ダイ上面から深さ d まで下げるのに必要な、曲げ線からの片側距離）：

| 深さ | V12 | V25 | V32 | V40 |
|---|---|---|---|---|
| 0 | **8.0** | **14.75** | **30** | **30** |
| 45 | 8.0 | 14.75 | 37 | 37 |
| 50 | 14.5 | 14.75 | 39.8 | 39.8 |
| 62.5 | 18.5 | 18.5 | 47 | 47 |
| 96.5 | 68 | 68 | 47 | 47 |
| 100 | 68 | 68 | 107 | 107 |
| 128 | 68 | 68 | 124（ナット） | 124 |
| 186.5 | 128 | 128 | 124 | 124 |
| 236.5 | 208 | 208 | 124 | 124 |
| 床 | 1061.5 | 1061.5 | 1048.0 | 1046.6 |

**ユーザー確認済みの前提**
- 図面の 825／900 は**図示寸法そのまま**。金型を載せる面がどこかは未確認なので使わない
- 深さ128〜148の幅124はナット（ボルト留め）。金型ではないが板は当たるので障害物として残す。900はナット下場から
- V32=1048.0 / V40=1046.6 は機械差として別々に保持
- ヤゲンは904061固定ではなく、この図がその組み合わせを描いているだけ

### 10.4 干渉判定の修正

接触（すきま0）を干渉と誤判定していた。平板がダイ上面に載っている状態は正常なので、
しきい値を「侵入 0.05mm 超」に変更。これで V32 t3.2 の単純1曲げが通るようになった。

### 10.5 成果物

`docs/曲げ可否判断シート.html` を4型ぶんに作り直し。不可の理由はA〜Dの4分類。

| | 理由 | 基準 |
|---|---|---|
| A | フランジが短すぎて溝に落ちる | 折り曲げ表の最小外寸／幾何限界 0.707×V |
| B | 立ち上がった面がヤゲンに当たる | ヤゲン11本の逃げ包絡線（DXF照合済み） |
| C | 下がる面がダイ・台に当たる | 4型の下逃げ包絡線（DXF実測） |
| D | Z曲げの段差が小さくて抜けない | 4型×ヤゲン11本の総当り |

**旧・理由E（ストローク／出し入れ）は削除。** オープンハイトの基準面が未確定のため。

Z曲げ最小段差（展開・中央辺）：V12＝19.3〜19.6 / V25＝19.3〜21.5 / V32＝47.9〜50.1 / V40＝48.2〜51.6。
V32・V40 が2倍以上必要なのは、ダイ本体が片側30mmあるため（理由C）。

その他の成果物：`docs/照合_図面vsモデル.png`、`docs/下逃げ表_V12_V25_V32_V40.csv`

### 10.6 次に決めること

1. A〜Dの4分類で足りるか（現場のNG実例と照合）
2. V40の最小フランジ。折り曲げ表の25〜30が幾何限界25.2〜27.4を下回る。実績優先でよいか
3. 次に追加する金型（取付図があるものから）
4. オープンハイトの基準面（決まれば理由Eを戻せる）

## 11. Z曲げの誤判定（曲3.dxf・曲4.dxf で原因特定）2026-09-02

### 症状
実際には曲がるZ（V12・t2.3・外寸 A42／段差12.3／B32）が、シートで
「✕ どの組み合わせでも曲がりません（2曲げ目 0.85mm 食い込み）」になった。

### 原因
**展開値（ブランク）をそのまま曲げ上がりの形状として干渉判定に入れていた。**

- 展開への換算（外寸 − 片伸び×隣接曲げ数）は正しい。→ [30, 8.3, 40]、合計78.3mm
- しかし実際に曲げると、曲げた箇所の両隣の辺は
  **シャープコーナー換算で（片伸び − 板厚/2）ずつ長くなる**。
  V12・t2.3・片伸び2 なら 1曲げにつき 2 − 1.15 = **0.85mm**。
- 判定はこの伸びを入れずに 8.3mm のまま計算していたので、
  段差が実物より 0.85mm 短く、上型に食い込む計算になっていた。

### 図面との照合（決め手）
| | 図面の実測 | 修正後のモデル |
|---|---|---|
| 曲4.dxf（1曲げ目だけ済み）V中心→曲1のシャープコーナー | 9.15mm | 9.15mm |
| 曲4.dxf 立下りフランジ内面 → ダイ側面 | 0.00mm（ギリギリ） | 0.00mm |
| 曲3.dxf（Z完成）各辺 | 30.85 / 10.00 / 40.85 | 30.85 / 10.00 / 40.85 |

8.3 → +0.85（1曲げ目後）→ 9.15 → +0.85（2曲げ目後）→ 10.00。
展開78.3 + 0.85×2 = シャープコーナー合計80.0。すべて一致。

段差寸法の定義も確定：**外寸法＝外面→外面（12.3）／内寸法＝内面→内面（7.7）**。
面→同じ側の面（10.0）は、曲げ上がりのシャープコーナー中央辺と一致する。

### 直したところ
1. `computeChain()` に `grow`（＝片伸び − 板厚/2）を追加。
   曲げ済みの曲げは 1.0、進行中の曲げはストローク進捗ぶんだけ両隣の辺を伸ばす。
   - `docs/曲げ可否判断シート.html`（script4.js）
   - `bending-simulator.jsx`：`part.grow = nobiPerBend.map(n => n - t/2)`
     （内寸モードは展開＝内寸の扱いなので t/2）
2. 結果表示に「判定に使う曲げ上がりの実寸」を追加。展開（切板）と併記。
3. 折り曲げ表に片伸びが無い板厚を選んだとき、△で「t○の値を仮置き」と警告するようにした。
4. **最小段差表を全面的に計算し直した**（旧値は上記バグ込みだった）。

| 下型 | 旧（誤） | 新 |
|---|---|---|
| V12 t1.2 / 1.6 / 2.3 | 19.3 / 19.3 / 19.6 | **12.5 / 12.6 / 12.8** |
| V25 t3.2 / 4.5 / 6 | 20.3 / 20.9 / 21.5 | **15.0 / 15.2 / 15.9** |
| V32 t3.2 / 4.5 / 6 | 48.8 / 49.4 / 50.1 | **46.2 / 47.0 / 48.0** |
| V40 t3.2 / 4.5 / 6 / 9 | 48.8 / 49.4 / 50.1 / 51.6 | **46.2 / 47.0 / 48.0 / 49.0** |

折り曲げ表に片伸びが無い組合せ（V25 t1.6・t2.3、V32 t1.6・t2.3、V40 t2.3）は
数字を出さず「—*」にした。片伸びが決まればツール側で出せる。

### 未確認
- 換算後の展開値が AP100 の展開図と一致するか（要照合）
- `dist/` は未ビルド。Windows 側で `npm run build` が必要。

### 追記：上向き判定（理由A〜C）も同じ修正
`evaluate()` も展開値をそのまま形状にしていたので `grow` を渡すよう修正。
あわせて入力を **外寸法／内寸法／展開値** の切替＋片伸び入力に変更し、
「展開（切板）」と「判定に使う曲げ上がりの実寸」を併記するようにした。
V肩に届くか（理由A）の判定も、工程2開始時の実寸（＝展開＋片伸び−t/2）で見るよう修正。

例：PL3.2 コの字 100×100×100（外寸）・鉄・V25・逃げ側向き
→ 展開 H96.8／W93.6、実寸 H98.4／W96.8。
10300・10400 でヤゲンまわり +2.9mm、10600 +1.0、904061 +0.2、00300 のみ −1.4 で不可。

## 12. コの字が曲がらない理由（曲5.dxf で特定）2026-09-02

### 症状
PL3.2 コの字 100×100×100 は実際には曲がらないのに、シートは「曲げられます」を出していた。

### 原因は3つ重なっていた

**1. 入力に立上りが1か所しかなかった**
コの字は立上りが2か所。H1（先に曲げた側）／W（底）／H2（これから曲げる側）の3入力に変更。

**2. 干渉の除外を「座標の近さ」でやっていた**
曲げている所は刃先とV肩に当たって当たり前なので除外していたが、その判定を
XY座標の距離でやっていたため、**離れた面がたまたまその位置に来ると見逃した**。
コの字の1枚目フランジが刃先の上を横切るのが、まさにこれ。
→ `splitChain()` を追加し、**板に沿った長さ（弧長）**で除外するよう変更。

**3. 上型ホルダがモデルに無かった（これが本命）**
2曲げ目の最後、底は45°に立つので、先に曲げた面が刃先の上を横切る。
PL3.2 100×100×100 では板が**刃先から上へ137.4mm**出る。

曲5.dxf の実測（V25・904061・90°完了時）：

| | 図面 | モデル |
|---|---|---|
| 刃先（=V底の板内面） | y=1198.791 | — |
| 板の最高点（フランジ先端 外面） | y=1335.604 → **136.8mm** | **137.4mm** |
| ヤゲン頂部 | y=1348.884 → 150.1mm（全高） | 150.1mm |
| フランジ先端の左右位置 | 中心から +4.8mm | 中心付近 |

板はヤゲン全高（150.1）は超えないが、**ホルダの中に入る**。

### ホルダ下端の高さは図面から読めた
ヤゲン11本の断面で、上部の細い部分（タング）の長さを測ると：

| 10300 | 10400 | 10600 | 71606 | 80306 | 904061 | 00300 | 00402 | 01002 | 04502 | 04702 |
|---|---|---|---|---|---|---|---|---|---|---|
| 30.0 | 30.0 | 30.0 | 30.0 | 30.0 | 30.0 | 30.0 | 30.0 | 30.0 | 30.0 | 30.0 |

**11本とも30.0mmちょうど。** ホルダはここを咥え、ヤゲン胴の上面がホルダ下端に座る。
よって **ホルダ下端 ＝ 刃先から（全高 − 30）mm**。904061 なら 120.1mm。
137.4 > 120.1 なので当たる。→ 11本すべてで ✕。実機と一致。

### 追加した判定「理由E ホルダに当たる」
- ホルダ下端＝全高−30（図面実測。手入力で上書き可）
- ホルダ幅＝**60mm は暫定**（図面にない。要実測）。幅の外は機械の前の空間なので当たらない扱い
- 判定は `riseInBand()`：ホルダ幅の中で板の外面が刃先より上へ何mm出るか

### 未確認
- **上型ホルダの幅**（現在60mm暫定）と、下端高さが本当に全高−30でよいか
- コの字の最大高さ表は、H と W の差でフランジの通り道が変わり単調でないため未作成

## 13. ホルダを図面実測に差し替え 2026-09-02

「ホルダって何？」→ 上型（ヤゲン）をラムに固定している金具のこと。
ヤゲンの上部の細い部分（タング・30mm）を咥えている。**曲5.dxf にも描いてあった**（私が見落としていた）。

### 使ったデータ
- `ホルダ.dxf`：ホルダ単体の閉多角形 **36点・幅95.22mm・全高150.70mm**。ヤゲン11本で共通
- `V12 ホルダあり.dxf`：V12＋ヤゲン904061＋ホルダの組図。位置決めの照合に使用

### 位置決め
タングの差込口（`ホルダ.dxf` の x31.02〜44.02・**幅13.0mm**）を、ヤゲンのタング（x −20.0〜−7.0・
**幅13.0mm**・11本とも同一）に合わせ、差込口の天面をヤゲン頂部に合わせた。
→ ホルダの最下端 ＝ 刃先から（ヤゲン全高 − 32.0）mm。

暫定でやっていた「幅60mm帯」「全高−30」は廃止。実形状をそのまま `minGap()` に渡している。

### 判定への反映
- `evaluate()` / `zFeasible()` とも、ダイ・ヤゲンと同じ `minGap()` でホルダも見る
- 断面図にホルダを描画。当たっている点に赤丸
- `maxH1()` はホルダに当たらない H1 の上限を返す

PL3.2 コの字100×100×100 は **ヤゲン11本すべてで ✕**（10300 で 39.2mm 食い込み）。実機と一致。

### 板厚→下型の自動選択（暫定）
板厚を変えると、折り曲げ表に片伸びの行がある型のうち **V≒8t に一番近いもの**を自動で選ぶ。

| t | 1.2 | 1.6 | 2.3 | 3.2 | 4.5 | 6 | 9 |
|---|---|---|---|---|---|---|---|
| 鉄 | V12 | V12 | V12 | V25 | **V40** | V40 | V40 |

**t4.5 が V40 になるのは要確認**（8t=36 に対し V32=32.01 / V40=39.93 で V40 のほうが近いため）。
`ベンダー折り曲げ金型寸法表.xlsx` の赤枠（基準金型）をもらえればそちらに差し替える。

### 未対応
- `bending-simulator.jsx` には理由E・弧長除外が未移植（シート側だけ先行）
- 上の t4.5 の自動選択

## 14. 断面図にホルダを表示 2026-09-02

「曲がるか曲がらないかはホルダに当たるか当たらないかが重要」との指摘を受け、
上型ホルダを**断面図に描く**ようにした。判定するだけでなく目で見て分かるようにするため。

- ホルダはタング下端から上へ45mmぶんを表示（それより上は切って平らに閉じる）。
  全高150.7mm すべて描くと縮尺が崩れて板が見えなくなるため
- 塗り・線はダイ・ヤゲンと同じ工具色。**当たっているときは赤**（`--ng-bg`／`--ng`）
- 食い込んでいる点に赤丸
- ラベル「上型ホルダ」を付記
- 上向き判定（`drawFigure`）とZ曲げ判定（`zDraw`）の両方に適用

あわせて、別セッションで作られた版から以下を取り込み：
- ホルダの位置決めを**タング下端＝ホルダ最下端**に統一（従来より2mm上。ヤゲン胴との重なりが解消）
- 板厚→基準金型の自動選択を、`ベンダー折り曲げ金型寸法表.xlsx` の赤枠セルの表に差し替え
  （鉄：t1.2/1.6/2.3→V12、t3.2/4.5→V25、t6/9/12→V40。従来の「V≒8tに最も近い」推定は廃止）
- Z曲げ側も板厚で下型を自動選択

### 未対応
- `bending-simulator.jsx` には理由E・弧長除外がまだ未移植
- `V12 ホルダあり.dxf` の組図に描かれたホルダは28点・幅78.3mmで、
  `ホルダ.dxf`（36点・幅95.2mm）より一回り小さい。判定は大きいほうを採用。どちらが現物か要確認

## 15. 板厚→基準金型の自動選択（赤枠を実読み）2026-09-02

`ベンダー折り曲げ金型寸法表 コピー.xlsx`（更新日 2022-04-01）のセル罫線の色を調べ、
**赤枠（FFFF0000）で囲われたセル＝基準金型**を抽出した。

| 板厚 | 1.2 | 1.6 | 2.3 | 3.2 | 4.5 | 5 | 6 | 8 | 9 | 10 | 12 | 16 | 19 | 22 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 鉄 | V12 | V12 | V12 | **V20** | V25 | V40 | V40 | V50 | V80 | V80 | V100 | V125 | V160 | V160 |

| 縞 | 2.3 | 3.2 | 4.5 | 6 | 9 | 12 |
|---|---|---|---|---|---|---|
|  | V25 | V40 | V40 | (V40) | V80 | V80 |

※縞6.0 だけ赤枠がないが、値があるのは V40 の列のみ。

### 選び方
1. 赤枠の型がこのシートの4型（V12/V25/V32/V40）にあれば、それを選ぶ
2. 無ければ（V20・V50・V80・V100…）、**折り曲げ表にその材質・板厚の行がある4型のうち、
   赤枠のV幅にいちばん近いもの**を選び、「基準は V20 だがシートに無いので V25」と明記する
3. どれにも行がなければ選ばず、その旨を表示（鉄t12＝基準V100、縞t9/t12＝基準V80）

結果（鉄）：t1.2/1.6/2.3→V12、**t3.2→V25（基準V20の代替）**、t4.5→V25、t6→V40、t9→V40（基準V80の代替）

### 直したバグ
初期表示で `state.die` が V32 のまま自動選択が走っていなかった。
`aAutoDie()` を初期化時にも呼ぶようにし、Z曲げ側にも同じ `zAutoDie()` を入れた。
下型は従来どおり手で変更できる（表の注意書き「赤枠以外を適用したい場合は折り曲げ可能かを確認すること」に対応）。

## 16. ホルダの上のラムを障害物に追加 2026-09-02

### 症状
PL6 コの字 500×500×500 が「曲げられます」になっていた。
実際は普通には曲がらず、**捨て曲げ**（底を一旦への字に曲げ→両サイドを90°→底を曲げ戻す）が要る。

### 原因
ホルダの断面だけを障害物にしていたため、**フランジがホルダの上を飛び越えると素通り**していた。

90°完了時の板（V40・t6・片伸び5）：
`(-351,-336) (0,16) (349,-334) (-2,-685)` ← 先端は刃先から **685mm 上・中心のほぼ真上**
ホルダは刃先から 118〜259mm の範囲。板の先端はその **426mm 上**を通っていた。

### 直し方
ホルダの上はラム（上部テーブル）なので、**ホルダの上端から上へ 4000mm の柱**を障害物に追加。
幅はホルダと同じ 95.2mm とした（実際のラムはもっと広いので、これは「少なくともここは当たる」の下限）。

- `placeRam()` を追加し、`evaluate()`・`zFeasible()` の両方でホルダと一緒に判定
- 断面図では破線の柱としてビューの上端まで描画。当たっていれば赤
- 理由Eの表示を「E 上型ホルダ・ラムに当たる」に変更
- ✕のときに **「捨て曲げなら通ることがある」** と併記するようにした
  （この判定は「普通に2工程で曲げられるか」であることを明記）

結果：PL6 コの字500×500×500 → **ヤゲン11本すべて ✕**（904061 で 49.3mm 食い込み）。実機と一致。

### 回帰確認（変化なし）
- PL3.2 コの字 100×100×100 → ✕（38.4mm 食い込み）
- PL3.2 コの字 100×300×100 → ○
- PL3.2 40×60×40 → ○（すきま 4.7mm）
- Z 曲3.dxf の実物 → ○（2曲げ目すきま 0.0mm）
- t1.6 Z の長物（B=1500mm）→ ○

### 未確認
- ラムの実際の幅（いまはホルダ幅で代用。広げるほど✕が増える方向）
- `bending-simulator.jsx` への移植（理由E・弧長除外・ラム）

## 17. ホルダ幅115mm・捨て曲げの成立条件 2026-09-06

現場からの実測値2つを反映。

### 1. ホルダ本体の幅は 115mm
`ホルダ.dxf` の断面は 95.2mm しか写っていなかった。実物は 115mm。
**ヤゲン頂部の 2mm 上から上は、幅115mmの柱**として扱うことにした
（`HOLDER_W=115`。柱はホルダ断面の中心に合わせて左右対称。その上はラム）。
タングの差込口はヤゲン頂部より下なので、ヤゲン本体を飲み込むことはない。

### 2. 捨て曲げの成立条件：曲げ戻しに 内-内で 120mm 必要
理由Eで✕になっても、内-内が 120mm 以上あれば
**捨て曲げ**（底を一旦への字に曲げ→両サイドを90°→底を曲げ戻す）で作れる。
内-内は `底の外寸 − 板厚×2`（＝曲げ上がりの実寸W − 板厚）で計算し、判定文に出す。
`SUTE_MIN_INNER=120`。

| 例 | 理由E | 内-内 | 判定文 |
|---|---|---|---|
| PL6 コの字 500×500×500 | ✕ 59.2mm 食い込み | 486.0 | 捨て曲げなら作れます |
| PL3.2 コの字 100×100×100 | ✕ 38.4mm 食い込み | 92.0 | 捨て曲げ（内-内120必要）も不可 |

### 回帰確認（変化なし）
PL3.2 100×300×100 → ○／PL3.2 40×60×40 → ○（すきま4.7mm）／
t3.2 30×40×60 → ✕（理由B）／Z 曲3.dxf → ○（2曲げ目 0.0mm）／t1.6 Z長物 B=1500 → ○

### 未対応
- `bending-simulator.jsx` への移植（理由E・弧長除外・ラム・ホルダ115mm）
- ホルダ断面そのもの（95.2mm）と実幅115mmの差。ヤゲン頂部より下は図面どおりの95.2mmで判定している

## 18. 特殊ヤゲン（くの字・首逃げ／中低）とボンデを追加（2026-09-06）

**もらった図面**
- `特殊ヤゲン.dxf` … 部品番号 00402。右が断面、左が正面図。正面図は 100+100＝200 と
  165+165＝330 の2種類あって、どちらも **中央だけ上を60mm落として高さ37mm**（両側 35×27 の
  テーパー、刃先から10mmは全幅）。両端65mmだけがタング付きの全高97mm。
- `曲がるコの字曲げ.dxf` … ボンデPL1.2、内寸 120×46.5×120、展開 120.2/46.9/120.2、
  片伸び **1.0**、曲げ長さ L=100、上90°×2、φ50×2。

**読み取り**
- 断面（27幅・全高97・タング13×30・首逃げ0.6〜0.9）は、すでにシートに入っている
  00402 と完全一致。標準の 00402 はそのまま残し、**特殊は別項目**として追加した。
- 中央の窓にワークを入れると、曲げ終わり90°で45°に寝てくる先曲げ立上り（120）は
  高さ37mmの中央部の上を素通りする。掴むのは両端のタングだけなので窓の上にホルダも無い。
  → L が窓（165版=200、100版=70）に収まれば、理由B・Eとも当たらない。

**入れたもの**
- 上型の選択肢に「特殊 くの字165（窓200）」「特殊 くの字100（窓70）」。断面は 00402 を
  高さ37mmで切ったもの（`clipLow`）。特殊を選ぶと `曲げ長さ L` の入力欄が出る。
- 特殊のとき理由Eは「窓に入る」に切替わり、ホルダ・ラムは判定しない。
- 普通のヤゲンで理由B/Eが✕のときは「特殊ヤゲンなら L◯◯mm 以下で曲がる」を併記。
- 材質に **ボンデ** を追加（片伸び t1.2＝1.0・V12。他の板厚は表に無いので「—」）。
- 基準金型の自動選択は、金型表にその板厚の行が無いときは近い値で埋めず「手で選んでください」に変更。

**確認**
- ボンデt1.2 内120/46.5/120・特殊165・L=100/160 → ○（B +0.1／窓200に収まる）
- 同 L=210、特殊100（窓70）でL=100 → ✕（窓オーバー）
- 同 標準00402 → ✕（B −7.0／E −30.9）
- 回帰：PL3.2 100/100/100 ✕、PL6 500/500/500 ✕、PL3.2 100/300/100 ○、PL3.2 40/60/40 ○

**未着手**
- ヤゲン 835mm×4本（最大3340mm）の長さ制限は未実装。
- `bending-simulator.jsx` への移植（理由E・弧長除外・ラム・ホルダ115mm・3辺入力・特殊ヤゲン）。

## 19. 言葉の訂正（2026-09-06）

- **「ラム」は使わない。** こちらが勝手に使った言葉で、現場では言っていない。
  ヤゲンより上は全部「**ホルダ**」と呼ぶ。シートの表記も全部直した。
- 判定の行の名前を、記号だけでなく中身がわかる言い方に変えた。
  - B「上で当たる」→ **「B ヤゲンに当たる」**
  - E「上型ホルダ・ラムに当たる」→ **「E ホルダに当たる」**
  - Eの文も「先に曲げた面が…」→「**倒れてきた立上りが**ホルダに◯◯mm食い込みます」
- 図 `docs/なぜ当たるか.png` を追加。板を置く→半分押す→90°まで押す、の3コマで
  「立上りが倒れてきて当たる」ところに丸を付けた。4コマ目が特殊ヤゲン（当たらない）。

## 20. ヤゲンの本数（2026-09-06・訂正）

- 通常ヤゲンは **1本835mm**。
- **HD3504NTは4mまで曲がるので4本では足りない。5本のときもある。**
  → 「835×4本＝最大3340mm」という制限は誤り。長さ制限はシートに入れていない。

## 21. 段差の最小は「計算」ではなく「実績」で持つ（2026-09-07）

**指摘**：t6・V25 の Z は **段差 外-外 30mm が最小**。1回曲げたときにできる**内R**の影響。

**原因**：このシートの計算は角をピン角（内Rゼロ）にしているので、実際より小さい段差でも
通ると出てしまう。t6・V25 だと計算では外-外25.5mmまで○になっていた（4.5mm甘い）。

**直した内容**
- 「最小段差表」から**計算で出した数字を全部消した**。代わりに `ZMIN`（下型×板厚の実績値・外-外）
  を持ち、分かっているマスだけ数字を出す。未登録は「—」。
- 入れた実績値：**V25・t6 ＝ 30mm**（今回）。他は全部「—」。
- Z曲げの判定に「段差の最小」の行を追加。実績値を下回ったら、計算の結果に関係なく✕にする。
  未登録のときは△で「実績値がまだ入っていません」と出す。
- 確認：V25・t6 で 段差外寸30→○（実績30に対し30）、29→✕、20→✕。V12・t2.3（実績未登録）→
  従来どおり計算だけで○。

**まだもらっていない値**：ZMIN の残りのマス（V12/V25/V32/V40 × t1.2〜t12）。
分かったものから順に埋める。

## 22. 折り曲げ表を読み直し・V63追加・段差の最小に内Rを入れる（2026-09-07）

**折り曲げ表を読み直したら、今まで4型ぶんしか見ていなかった。** 表には14型（V8〜V160）が載っていて、
さらに **曲げ内R** と **所有台数** も書いてあった。

| | V8 | V12 | V16 | V20 | V25 | V32 | V40 | V50 | V63 | V80 | V100 | V125 | V160 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 曲げ内R | 1.3 | 2 | 2.6 | 3.3 | 4 | 5 | 6.5 | 8 | 10 | 13 | 16 | 20 | 26 |
| 所有 | 5台 | 5台 | 5台 | 5台 | 5台 | **2台** | 5台 | 5台 | **1台** | 5台 | 5台 | 5台 | 5台 |

1台835mm。V63は1台＝835mmまで、V32は2台＝1670mmまでしか並べられない。
金型台帳に「下型ぜんぶ（14型）」の表を追加した。断面の実測がある型だけ「判定できる」と表示。

**V63 を取り込んだ**（`V63.dxf`）。ベッドの幅が±107mmで V32・V40 と完全一致したので **HD3504NT**。
V幅63.00／溝深さ32.97／床まで1063.0／下逃げは深さ0から片側40mm。
ダイ本体は図面の輪郭そのまま、それより下（深さ55以降）は深さごとの左右最大で階段状に置き換えた（安全側）。

**段差の最小に内Rを入れた。**
- 実績2件（V12・t2.3＝12.3／V25・t6＝30）に合う式は **3×(曲げ内R＋板厚)**。
  V25・t6 → 3×(4+6)=30 でぴったり。V12・t2.3 → 3×(2+2.3)=12.9（実績12.3、0.6の差）。
- 「最小段差表」を **実績／暫定** の2種類表示にした。実績があるマスは実績、無いマスは暫定値。
  判定でも同じ優先順。暫定と分かるように必ず「暫定」と書く。
- 幾何（当たり判定）の値と比べると、V12・V25では暫定のほうが大きく、V32・V40・V63では幾何のほうが
  大きい。実際の最小は両方の大きいほうになるはず。実績が増えたら式ごと見直す。

**シミュレーション結果（参考値）を記入シートに載せた**（`docs/Z曲げ_実績記入シート.xlsx`・1枚）。
最小フランジ・曲げ内R・暫定段差・幾何段差（フランジ短め／A=B=80）を灰色で並べ、黄色に実績を書き込む形。
フランジの「最大」は載せていない（下記の不具合のため）。

**見つかった不具合（未修理）**
- Z曲げの総当りで、**逆の曲げ順が常に「谷曲げ」と判定されて捨てられている**（88通りのうち実質半分しか
  見ていない）。そのせいでフランジAだけ上限97〜105mmが出て、フランジBは無制限という非対称になる。
  左右反転（mirror）が本当の鏡像になっていないのも同根。次に直す。

## 23. Z曲げの曲げ順の不具合を修正・フランジの最小と最大を計算（2026-09-08）

**不具合**：`zSearch` は 曲げ順2通り × 左右反転2通り × ヤゲン向き2通り × ヤゲン11本 ＝ 88通りを
試していたが、**板を裏返すかどうかを「2工程目だけ裏返す」に決め打ち**していた。
そのため「裏返してから2番目の曲げ線を先に曲げる」やり方をひとつも試していなかった。
Zの2つの曲げは向きが逆（dir +1 と -1）なので、逆順の1工程目は必ず谷曲げ判定になって捨てられていた。

**直し方**：裏返すかどうかを工程ごとに自由に選べるようにした（2工程 × 2通り ＝ 4通り）。
総当りは 88通り → 352通りに。

**結果**
- フランジAだけ上限97〜105mmが出てBは無制限、という非対称が消えた。A=800/B=80 も逆順で○になる。
- **段差Sの最小は変わらなかった**（V12・t2.3＝12.3、V25・t6＝25.6 など、公開済みの値そのまま）。
- 回帰も全部そのまま：曲3.dxf（t2.3・V12・42/12.3/32）→ ○ 2曲げ目すきま0.0mm。
  コの字の5件、PL6 500³、PL3.2 100³ なども変化なし。

**フランジの最小と最大（Z曲げ・断面判定）**

| 下型 | t | 最小フランジ（外寸） | 最大 |
|---|---|---|---|
| V12 | 1.2/1.6/2.3 | 9.6 / 9.7 / 9.9 | なし |
| V25 | 3.2/4.5/6 | 19.8 / 20.0 / 20.0 | なし |
| V32 | 3.2/4.5/6 | 25.6 / 25.4 / 25.2 | なし |
| V40 | 3.2/4.5/6/9 | 31.2 / 31.0 / 30.8 / 31.3 | なし |
| V63 | 9 | 48.6 | なし |

**最大は断面の判定では出ない。** 長いフランジは平らに寝ているだけで、当たる相手がいないため。
実際の上限は機械の奥行き・持ち方・自重など断面では見えない理由で決まるので、実績が要る。
記入シートの注記もそう書き換えた。

## 24. bending-simulator.jsx への移植（フェーズ1・コア幾何）2026-09-09

`棚卸し_jsx移植と多曲げ設計_20260909.md` のフェーズ1（E1・E2・E4・E5・E6・E11）を実装した。
ホルダ・実測台・理由E・テーブル類（フェーズ2〜4）はまだ未移植。

### 移植した内容

- **`computeChain` に `grow`（片伸び−板厚/2）を追加**（第11章）。曲げ済みの辺・進行中の辺の
  両隣がシャープコーナー換算で伸びるようにした。`part.growArr`（曲げごとの伸び量）を
  `bdList`（既存のK係数式による伸び値）から `bd/2 − t/2` で暫定算出。
  **実測の折り曲げ表値（片伸び）に未対応**——これは第18章で挙げたテーブル移植（フェーズ3）待ち。
  検証：V12・t2.3・grow=0.85 のZ曲げで、中央辺が 8.3 → 9.15（1曲げ目後）→ 10.00（完成）と、
  第11章の実測値と1e-3mm未満で一致することを確認。
- **中立軸オフセット・刃先高さのバグ修正**（第8.1章バグ2・3）。`vy`／`innerY` を面直オフセットで
  再計算し、`buildPunch` の刃先高さを `ch.vy − t/2` から `ch.innerY` に統一（3箇所）。
- **`mirrorClose` の index0 バグ修正**（第9.3章）。先頭点も鏡像に含めるよう変更。
  `INSERT_STACK_RIGHT`（先頭 `[14,45]`）で境目が斜め線ではなく水平な天面になることを確認。
- **最小フランジ判定 `shoulderReach` / `reachCheck` を追加**（第8.2章）。
  `computeChain` は届く／届かないに関わらず肩支点で回転させるため、reach判定は別立てにした。
  工程バッジに「✕ フランジ不足(◯mm必要)」を表示。
- **弧長ベースの正規接触除外 `splitChain`／`minGap` を追加**（第12章）。
  旧 `detectCollisions`（座標距離で除外・侵入点の配列を返すだけ）を撤去し、
  `minGap`（最小すきまmm・負＝食い込み量、可視化用の侵入点リストも返す）に置き換えた。
  `bboxOf`／`clipSeg`（Liang–Barsky）／`sampleInBox` も移植（計算量を抑えるための工具外接枠クリップ）。
- UI表示を「干渉◯点」から「食い込み◯mm」「余裕◯mm」に変更。

### 未移植（フェーズ2以降）

- 上型ホルダ・その上の柱（理由E）、実測ダイ台（4型＋V63）、下逃げ量の専用UI
- 折り曲げ表の片伸び・基準金型テーブル、V63、Z段差の実績優先化（`ZMIN`）
- Z総当りの工程別裏返し、特殊ヤゲン（中低）、捨て曲げ成立条件

上記フェーズの状況は `棚卸し_jsx移植と多曲げ設計_20260909.md` を参照。

## 25. 分岐していた jsx のマージと、シートの扱いの方針（2026-09-10）

### 何が起きていたか

第24章のフェーズ1移植（`1d884dc`）は、**7月版の `bending-simulator.jsx`（1113行）を土台に**行われていた。
一方このPCの作業ツリーには、同じ7月版から別方向に育った未コミットの jsx（1652行）があり、
pull が衝突して初めて双方に相手の持たない成果があることが分かった。

- **上流だけが持っていた**：`splitChain`／`minGap`／`reachCheck`／`vIdx`
  （`bboxOf`・`clipSeg`・`sampleInBox`・`shoulderReach` はローカルにも既にあった）
- **ローカルだけが持っていた**：`grooveVHalf`（第8.1章(1)の修正）、実測片伸び表 `NOBI_TABLE`、
  最小外寸表 `MINOUT_TABLE`、曲げ内R `V_INNER_R`、材質（鉄／縞）、実測ダイ台 `DIE_MOUNT`（4通り）、
  寸法表示、ストロークの開き

コミット日時では新旧を判定できない。**関数の有無で見るのが正しい。**

### 金型名の後退（重要）

上流版は `vHalf` に `DIE_LIB[].grooves[1]`（肩R外端）をそのまま使い、金型名もその2倍にしていた。
これは第8.1章(1)で「旧 vHalf×2」として**直したはずの値**である。

| 品番 | カタログ呼称（正） | 上流 `1d884dc` の表示 |
|---|---|---|
| 03500 | V32 | V35.5 |
| 03600 | V40 | V43.5 |
| 03700 | V50 | V53.5 |
| 03800 | V63 | V67 |
| 01360 | V80 | V85 |
| 01860 | V100 | V106 |
| 03900 | V125 | V133 |
| 01400 | V160 | V170 |
| 34300 | V40鋭角 | V47鋭角 |

マージではローカル版の `grooveVHalf()` とカタログ呼称を採用し、この後退を持ち込まなかった。

### マージの内容（`2ff06cc`）

ローカル版を土台に、上流からコア幾何の4点だけを移植した。

- `splitChain`／`minGap` を移植し、旧 `detectCollisions` を撤去
- `computeChain` の戻り値に `vIdx` を追加
- `reachCheck` を移植（曲げ済みの辺を巻き込んだ長さでV肩到達を判定）
- 上流にしかなかった V6インサート（970061）を取り込み
- 表示を「余裕◯mm／食い込み◯mm／最小すきま◯mm」に統一

結果 1719行。`npm run build` 通過。dev サーバーで、V6インサートのV半幅が 3.00 と算出されること、
片伸び表が引けること（鉄・V12）、t3.2 のコの字で2工程目に食い込み 3.7mm を検出することを確認した。

### 第24章の「未移植」リストの訂正

第24章で未移植としていたもののうち、実際にはローカル側に入っていてマージで載ったものがある。

- **入っていた**：折り曲げ表の片伸び、最小外寸、曲げ内R、材質（鉄／縞）、V63の断面、
  実測ダイ台4通り（974061・982061・03500・03600）
- **まだ未移植**：上型ホルダとその上の柱（理由E）、V63の台、基準金型の自動選択（`BASEV`／`pickDie`）、
  Z段差の実績優先（`ZMIN`）、下逃げの深さ別表、特殊ヤゲン（中低）、捨て曲げ成立条件

### シートはリポジトリ側で編集しない（方針）

`0dbf4bf` でシートの幾何エンジンのコメントをリポジトリ側で直したが、このシートは
**Claude の会話側で生成されブラウザ経由で Downloads に落ちてくる**ものなので、その修正は生成元へ戻らない。
実際 2026-09-10 の再ダウンロードで修正前の内容が出てきて、どちらが新しいのか判別できなくなった
（`5e8d327` の blob とダウンロード版がともに 140,622 B、`0dbf4bf` だけが +369 B ずれていた）。

**今後シートはダウンロード版をそのまま置くだけにする。** 文面の訂正はリポジトリでは行わず、
生成元へ渡して次の生成物に反映する。`7e10137` で `5e8d327` と同一 blob へ戻した。

jsx とシートで同じ形の行き違いが二度起きている。片方で直したものは、もう片方には戻らない。
